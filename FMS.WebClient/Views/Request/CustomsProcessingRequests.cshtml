@model RequestListingViewModel;
@{
    ViewData["Title"] = "CustomsProcessingRequests";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Customs processing</h1>

<div>
    <a href="/Request/CustomsProcessingRequests?page=@Model.PreviousPage" class="float-left btn bz-btn-info @(Model.PreviousDisabled?"disabled":"")">Prev</a>
    <a href="/Request/CustomsProcessingRequests?page=@Model.NextPage" class="float-right btn bz-btn-info @(Model.NextDisabled?"disabled":"")">Next</a>
</div>
<table class="table table-bordered table-striped">
    <tr>
        <th>Number</th>
        <th>Date create</th>
        <th>From</th>
        <th>To</th>
        <th>Info</th>
        <th></th>
    </tr>
    @foreach (var item in Model.list)
    {
        <tr>
            <td>@item.Number</td>
            <td>@item.DateCreate</td>
            <td>@item.FromCountryCity</td>
            <td>@item.ToCountryCity</td>
            <td>@item.LoadName</td>
            <td>
                <a href="/Document/CreateInvoice?requestID=@item.ID" class="btn bz-btn-info">Invoice</a>
            </td>
        </tr>
    }
</table>